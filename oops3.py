# here computer class is a blue print (design)and it must contains some object
class computer:
    pass
    
# concept of heap memory where all object are placed.
# every time we create an object is allocated new space in memory

c1= computer()
c2= computer()
print(id(c1))
print(id(c2))
